import { readExcel } from "@/utils";
import { IFilesExcelDataListItem } from ".";

export function handleExcelData(filesExcelDataList: IFilesExcelDataListItem[]){
  // 接下来一个一个的遍历文件 然后解析import 相当于DFS遍历
}
